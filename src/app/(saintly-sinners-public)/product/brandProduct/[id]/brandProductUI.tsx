import { ProductAxiosType } from '@/interfaces/product.type';

export default function HomeUI({
	products,
	children,
}: {
	products: ProductAxiosType[];
	children: React.ReactNode;
}) {
	return <div> brand product</div>;
}
